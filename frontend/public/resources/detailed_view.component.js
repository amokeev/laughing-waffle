// main List view
window.DetailedViewComponent = React.createClass({
    // render component on the page
    render: function() {
        $('.page-header h1').text('Detailed View');
        return (
            <div className='overflow-hidden'>
                Detailed view implementation is TBD.
            </div>
        );
    }
});
